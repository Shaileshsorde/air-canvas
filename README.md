# air-canvas
Air Canvas is a virtual drawing application that allows users to draw on the screen without touching it. It uses a webcam to detect a colored object (blue color range defined in HSV), tracks its movement, and draws lines on a digital canvas based on the object’s motion.
# 🖌️ Air Canvas

Air Canvas is a virtual drawing application that allows users to draw on the screen without touching it.  
It uses a webcam to detect a colored object (blue color range defined in HSV), tracks its movement, and draws lines on a digital canvas based on the object’s motion.

---

## 🚀 Features

- Real-time object tracking using OpenCV
- Draw without touching the screen
- HSV color detection
- Smooth line drawing
- Clear canvas option

---

## 🛠️ Technologies Used

- Python 3.x
- OpenCV
- NumPy

---

## 💻 Installation

1. Clone the repository:https://github.com/Shaileshsorde/air-canvas.git
2. Navigate to project folder:cd air-canvas
Install required libraries:pip install opencv-python numpy
Run the project:python main.py
🎮 How It Works

Hold a blue colored object in front of the webcam.

The camera detects the object using HSV color detection.

Move the object in air to draw.

Press 'C' to clear the canvas.

Press 'Q' to quit.

📂 Project Structure

main.py → Main application file

README.md → Project documentation

📸 Output Preview

(Add a screenshot here)

![Air Canvas Output](screenshot.png)

📄 License

This project is for educational purposes.


---

## 🔥 Extra (Very Important for Resume)

If you are adding this to resume, also include:

- Problem Statement  
- Future Improvements (like multiple color support, brush size control)

Example:

```markdown
## 🔮 Future Improvements
- Multiple color selection
- Brush thickness control
- Save drawing as image


   ```bash
   git clone https://github.com/your-username/air-canvas.git
